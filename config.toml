# The URL the site will be built for
base_url = "https://aevyrie.github.io"

# Whether to automatically compile all Sass files in the sass directory
compile_sass = true

# Whether to do syntax highlighting
# Theme can be customised by setting the `highlight_theme` variable to a theme supported by Zola
highlight_code = true
highlight_theme = "demain"

# Whether to build a search index to be used later on by a JavaScript library
build_search_index = false

theme = "simple-dev-blog"

title = "Option::Some(Blog)"
description = "A simple dev blog."

generate_feed = true

taxonomies = [
  { name = "tags" }
]

[extra]

nav = [
  { name = "About", path = "/about/" },
  { name = "Home", path = "/" }
]

latest_text = "Latest post:"

icon = "icon.png"
profile_small = "icon.png"
profile_large = "profile.jpg"

accent = "#087EA5"
accent_light = "#ddd"

author = "John Doe"

footer_about = """
A simple dev-blog theme created by Bennett. You can follow him on [Github](https://github.com/bennetthardwick) (if you like).
"""

not_found_message = """
This is not the page you're looking for... return back to [safety?](/)
"""